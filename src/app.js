'use strict';
console.log('app.js');

import { minutes } from '../helper';

const name = 'James';

const fulName = `${name} Bond`;

console.log('fulName ===', fulName);

console.log(minutes(12));